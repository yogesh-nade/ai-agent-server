install dependencies 
cd Agent0
npm install


# send a post request to 
http://localhost:3000/chat

in body 
{
  "message": "How many users are in the database?"
}

<!-- You can now test your AI agent with queries like:
   - "How many users are in the database?"
   - "Show me all software engineers"
   - "Find users in California"
   - "What's the average salary?" -->

# GitHub Repository:
 https://github.com/yogesh-nade/ai-agent-server


# Check status
git status

# Stage and commit changes
git add .
git commit -m "Your commit message"

# Push to GitHub
git push

# Pull latest changes
git pull

# Create a new branch for features
git checkout -b feature/new-feature

# View commit history
git log --oneline